# intent-based-chatbot
This repository contains an intent-based chatbot built using a fine-tuned BERT model for intent classification. The backend handles model training and inference using FastAPI, while the frontend provides a responsive React chat interface for user interaction.
